# Vehicle Bookings

---

# Basic CRUD Routes

```http
GET /bookings

GET /bookings/:bookingId

POST /bookings

PUT /bookings/:bookingId

PATCH /bookings/:bookingId/status

DELETE /bookings/:bookingId
```

---

# Route Parameters Practice

```http
GET /bookings/id/:bookingId

GET /bookings/status/:status

GET /bookings/customer/:customerId

GET /bookings/vehicle/:vehicleType

GET /bookings/payment/:method

GET /bookings/pickup/:location

GET /bookings/drop/:location

GET /bookings/date/:date

GET /bookings/time/:time

GET /bookings/rating/driver/:rating

GET /bookings/rating/customer/:rating

GET /bookings/distance/:distance

GET /bookings/value/:amount

GET /bookings/incomplete/:status

GET /bookings/incomplete-reason/:reason

GET /bookings/cancel/customer/:reason

GET /bookings/cancel/driver/:reason

GET /bookings/vtat/:minutes

GET /bookings/ctat/:minutes

GET /bookings/day/:day

GET /bookings/month/:month

GET /bookings/year/:year

GET /bookings/hour/:hour

GET /bookings/minute/:minute

GET /bookings/source/:pickup

GET /bookings/destination/:drop

GET /bookings/vehicle-image/:imageName

GET /bookings/fare/:value

GET /bookings/customer/:customerId/history

GET /bookings/customer/:customerId/latest
```

---

# Query Parameter Practice

```http
GET /bookings?status=Success

GET /bookings?vehicle=Bike

GET /bookings?payment=UPI

GET /bookings?pickup=Indiranagar

GET /bookings?drop=Jayanagar

GET /bookings?date=2024-07-26

GET /bookings?time=14:00

GET /bookings?driverRating=4

GET /bookings?customerRating=5

GET /bookings?minFare=200

GET /bookings?maxFare=1000

GET /bookings?minDistance=10

GET /bookings?maxDistance=40

GET /bookings?customer=CID123456

GET /bookings?incomplete=Yes

GET /bookings?cancelledByDriver=true

GET /bookings?cancelledByCustomer=true

GET /bookings?sort=Booking_Value

GET /bookings?sort=-Booking_Value

GET /bookings?vehicle=Mini&status=Success

GET /bookings?pickup=HSR Layout&drop=BTM Layout

GET /bookings?payment=Cash&vehicle=Bike

GET /bookings?minRating=4&maxRating=5

GET /bookings?distanceAbove=20

GET /bookings?distanceBelow=10

GET /bookings?month=07

GET /bookings?year=2024

GET /bookings?hour=18

GET /bookings?status=Driver Not Found

GET /bookings?status=Canceled by Driver
```

---

# Pagination Routes

```http
GET /bookings?page=1&limit=10

GET /bookings?page=2&limit=20

GET /customers?page=1&limit=10

GET /vehicles?page=1&limit=5

GET /success-rides?page=1&limit=15

GET /cancelled-rides?page=2&limit=10

GET /incomplete-rides?page=1&limit=5

GET /ratings?page=1&limit=25

GET /payments?page=1&limit=20

GET /admin/bookings?page=1&limit=50

GET /logs?page=1&limit=100

GET /search?page=2&limit=10

GET /rides/history?page=3&limit=15

GET /customers/top?page=1&limit=10

GET /drivers/top-rated?page=1&limit=5
```

---

# Sorting APIs

```http
GET /bookings?sort=Booking_Value

GET /bookings?sort=-Booking_Value

GET /bookings?sort=Ride_Distance

GET /bookings?sort=-Ride_Distance

GET /bookings?sort=Driver_Ratings

GET /bookings?sort=-Driver_Ratings

GET /bookings?sort=Customer_Rating

GET /bookings?sort=-Customer_Rating

GET /bookings?sort=Date

GET /bookings?sort=-Date

GET /bookings?sort=Vehicle_Type

GET /bookings?sort=Payment_Method

GET /bookings?sort=Pickup_Location

GET /bookings?sort=Drop_Location

GET /bookings?sort=Booking_Status
```

---

# Search APIs

```http
GET /search?keyword=Indiranagar

GET /search?keyword=Bike

GET /search?keyword=Prime SUV

GET /search?keyword=Canceled

GET /search?keyword=UPI

GET /search/bookings?bookingId=CNR123456

GET /search/customers?customerId=CID123456

GET /search/payment?method=Cash

GET /search/vehicle?type=Mini

GET /search/location?pickup=HSR Layout

GET /search/location?drop=Jayanagar

GET /search/cancel-reason?reason=Customer

GET /search/incomplete?reason=Vehicle Breakdown

GET /search/rating?driver=4.5

GET /search/rating?customer=5

GET /search/date?date=2024-07-20

GET /search/month?month=07

GET /search/fare?value=500

GET /search/distance?km=20

GET /search/status?status=Success
```

---

# Combined Query + Pagination + Sorting

```http
GET /bookings?status=Success&page=1&limit=10&sort=-Booking_Value

GET /bookings?vehicle=Bike&page=2&limit=5

GET /bookings?payment=UPI&sort=-Ride_Distance

GET /bookings?pickup=Indiranagar&page=1&limit=20

GET /bookings?drop=Jayanagar&sort=Customer_Rating

GET /bookings?minFare=500&maxFare=2000

GET /bookings?status=Canceled by Driver&sort=-Date

GET /bookings?vehicle=Mini&payment=Cash

GET /bookings?driverRating=4&page=1&limit=10

GET /bookings?distanceAbove=20&sort=-Ride_Distance

GET /bookings?month=07&page=2&limit=10

GET /bookings?year=2024&sort=-Booking_Value

GET /bookings?customerRating=5&page=1&limit=5

GET /bookings?pickup=Koramangala&drop=BTM Layout

GET /bookings?status=Success&vehicle=Prime SUV
```

---

# POST Routes

```http
POST /bookings

POST /customers

POST /drivers

POST /payments

POST /ratings

POST /vehicles

POST /locations

POST /bookings/bulk-insert

POST /customers/bulk-insert

POST /drivers/bulk-insert

POST /payments/bulk-insert

POST /ratings/bulk-insert

POST /import/json

POST /import/csv

POST /reports/generate
```

---

# PUT/PATCH Routes

```http
PUT /bookings/:bookingId

PUT /customers/:customerId

PUT /drivers/:driverId

PUT /vehicles/:vehicleId

PATCH /bookings/:bookingId/status

PATCH /bookings/:bookingId/payment

PATCH /bookings/:bookingId/rating

PATCH /bookings/:bookingId/fare

PATCH /bookings/:bookingId/distance

PATCH /bookings/:bookingId/location

PATCH /bookings/:bookingId/cancel-reason

PATCH /customers/:customerId/profile

PATCH /drivers/:driverId/status

PATCH /vehicles/:vehicleId/type

PATCH /payments/:paymentId/status
```

---

# DELETE Routes

```http
DELETE /bookings/:bookingId

DELETE /customers/:customerId

DELETE /drivers/:driverId

DELETE /vehicles/:vehicleId

DELETE /payments/:paymentId

DELETE /ratings/:ratingId

DELETE /logs/:id

DELETE /bookings/delete-all

DELETE /customers/delete-all

DELETE /cancelled-rides/delete-all

DELETE /incomplete-rides/delete-all

DELETE /bookings/date/:date

DELETE /bookings/month/:month

DELETE /bookings/year/:year

DELETE /logs/clear
```

---

# Statistics Routes

```http
GET /stats/total-bookings

GET /stats/success-rides

GET /stats/cancelled-rides

GET /stats/incomplete-rides

GET /stats/driver-not-found

GET /stats/total-customers

GET /stats/top-vehicle

GET /stats/top-payment-method

GET /stats/highest-fare

GET /stats/lowest-fare

GET /stats/average-fare

GET /stats/average-distance

GET /stats/average-driver-rating

GET /stats/average-customer-rating

GET /stats/most-common-pickup

GET /stats/most-common-drop

GET /stats/monthly-bookings

GET /stats/yearly-bookings

GET /stats/top-customers

GET /stats/top-locations
```

---

# Middleware Practice Routes

```http
GET /admin/bookings

POST /admin/bookings

DELETE /admin/bookings/:bookingId

PATCH /admin/bookings/:bookingId

GET /admin/dashboard

GET /protected/bookings

POST /protected/bookings

DELETE /protected/bookings/:bookingId

PATCH /protected/bookings/:bookingId

GET /admin/customers

DELETE /admin/customers/:customerId

PATCH /admin/customers/:customerId/block

PATCH /admin/customers/:customerId/unblock

GET /admin/logs

GET /admin/reports
```

---

# Authentication Practice Routes

```http
POST /auth/register

POST /auth/login

POST /auth/logout

POST /auth/forgot-password

POST /auth/reset-password

POST /auth/refresh-token

GET /auth/me

DELETE /auth/account

POST /auth/verify-email

POST /auth/send-otp

POST /auth/verify-otp

POST /auth/change-password

POST /auth/logout-all

GET /auth/sessions

DELETE /auth/sessions/:id
```

---

# JWT Authentication Routes

```http
GET /jwt/profile

GET /jwt/dashboard

POST /jwt/generate-token

POST /jwt/verify-token

POST /jwt/refresh-token

GET /jwt/admin

GET /jwt/user

DELETE /jwt/logout

GET /jwt/verify-admin

GET /jwt/verify-user

POST /jwt/revoke-token

GET /jwt/current-user

GET /jwt/is-authenticated

GET /jwt/check-role/admin

GET /jwt/check-role/user
```

---

# Error Handling Practice

```http
GET /bookings/:bookingId
→ Handle invalid booking ID

GET /bookings/:bookingId
→ Handle booking not found

POST /bookings
→ Handle missing required fields

POST /bookings
→ Handle duplicate booking ID

PATCH /bookings/:bookingId
→ Handle invalid update data

DELETE /bookings/:bookingId
→ Handle already deleted booking

GET /admin/bookings
→ Handle unauthorized access

POST /auth/login
→ Handle invalid credentials

GET /protected/bookings
→ Handle missing JWT token

GET /admin/dashboard
→ Handle forbidden access
```

---

# Request Validation Practice

```http
POST /bookings

→ Validate booking ID format

→ Validate booking date required

→ Validate customer ID required

→ Validate vehicle type required

→ Validate booking value numeric

→ Validate distance numeric

→ Validate pickup location required

→ Validate drop location required

→ Validate payment method enum

→ Validate ratings between 1 to 5

PATCH /bookings/:bookingId
→ Prevent invalid fields update

POST /auth/register
→ Validate strong password

POST /auth/login
→ Validate email/mobile format
```

---

# API Rate Limiting Practice

```http
GET /bookings
→ Limit requests per minute

POST /auth/login
→ Prevent brute force attacks

POST /auth/register
→ Limit registration requests

GET /search
→ Limit excessive searches

GET /admin/dashboard
→ Strict admin rate limiting

POST /bookings
→ Prevent spam submissions

DELETE /bookings/:bookingId
→ Limit delete requests

POST /import/json
→ Limit bulk uploads

GET /stats
→ Max 20 requests per minute

GET /search
→ Max 30 requests per minute
```

---

# Export & Import Routes

```http
GET /export/json

GET /export/csv

GET /export/excel

GET /export/pdf

GET /export/bookings

GET /export/stats

POST /import/json

POST /import/csv

POST /import/excel

POST /backup/create

POST /backup/restore
```

---

# Bonus Practice Routes

```http
GET /bookings/top/highest-fare

GET /bookings/top/lowest-fare

GET /bookings/recent

GET /bookings/latest

GET /bookings/random

GET /bookings/trending

GET /bookings/success

GET /bookings/cancelled

GET /bookings/incomplete

GET /bookings/driver-not-found

GET /health

GET /version

GET /compare?booking1=CNR111&booking2=CNR222

GET /bookings/summary/ai

GET /bookings/recent/week

GET /bookings/recent/month

GET /bookings/recent/year

GET /bookings/top/customers

GET /bookings/top/vehicles
```

---
